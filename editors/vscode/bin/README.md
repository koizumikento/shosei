# Bundled CLI staging

Platform release jobs copy the matching `shosei` executable into this directory before packaging a target-specific VSIX. Executables are generated release artifacts and must not be committed.
